name: MemeForge Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Syntax check browser source
        run: |
          node --check src/app.js
          node --check src/template-ui.js
          node --check src/data/templates.js
          node --check src/engines/storage-engine.js
          node --check src/engines/template-engine.js
          node --check react-app/src/canvas.js
          node --check react-app/src/remix-history.js
          node --check react-app/src/useStudioHistory.js

      - name: Run engine and wiring tests
        run: npm test
